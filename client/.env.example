# Relative base URL — dev uses the Vite proxy, prod uses the Vercel rewrite,
# so the API is always same-origin and auth cookies stay first-party.
VITE_API_URL=/api/v1

# Dev-only: where the Vite proxy forwards `/api`. Defaults to the deployed API.
# VITE_PROXY_TARGET=http://localhost:3000
